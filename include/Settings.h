/**
 * This is a Setting of th OS,
 * you can adjust some other features that do not belong to real OS 
 * wiht following "#define".
 */

// @1: open or close log function.
#define USE_LOG

// @2: flush file system every time when OS restart
// #define RESTART_FS